{
  "name": "react-virtual-list-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter react-virtual-list build && pnpm --filter playground build",
    "build:lib": "pnpm --filter react-virtual-list build",
    "build:playground": "pnpm --filter playground build",
    "typecheck": "pnpm --filter react-virtual-list typecheck && pnpm --filter playground typecheck"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
